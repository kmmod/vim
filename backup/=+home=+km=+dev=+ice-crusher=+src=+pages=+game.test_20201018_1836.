import React from 'react';
import { render } from '@testing-library/react'
import Game from './game'


test('Renders game field', () => {
  render(<Game/>)
})

test('Check random game field generation', ()=> {
  const renderer = render(<Game/>)
  
})
