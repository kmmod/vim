const { getRandomTile } = require("./gameFieldManager");

describe('Check if random within correct range', ()=>{
  it('should work for 0 size', ()=>{

 expect(getRandomTile(0)).toStrictEqual([0,0]);

  })
  it('should not exceed maximum size', ()=>{

    
  })
})
